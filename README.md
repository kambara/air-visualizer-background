# Background image generator for BOS

## Install

```
pipenv install
```

## Usage

The following command will generate an image of the specified width in the `images` folder.

```
pipenv shell
python main.py [width]
```
